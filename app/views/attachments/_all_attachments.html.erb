
<% @attachments.each do |attachment| %>
	<div class="image-text m-2">
        <%= image_tag(attachment.image.thumb.url) %>
        <div class="centered">
          <%= link_to "Show" , attachment, class: "btn-image", remote: true %>
        </div>
      </div>
<% end %>

